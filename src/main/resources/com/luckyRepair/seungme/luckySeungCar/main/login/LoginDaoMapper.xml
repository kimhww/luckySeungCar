<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

    <mapper namespace="com.luckyRepair.seungme.luckySeungCar.main.login.LoginDao">

        <select id="findUserById" parameterType="string" resultType="com.luckyRepair.seungme.luckySeungCar.main.login.domain.LoginDomain">
            select user_idnt,
                   user_pswd,
                   grup_code,
                   logn_date,
                   logn_ipas
                from user_base
                where user_idnt = #{userIdnt}
        </select>

        <update id="updateLoginInfo" parameterType="com.luckyRepair.seungme.luckySeungCar.main.login.domain.LoginDomain">
            update user_base
                set logn_date = #{lognDate},
                    logn_ipas = #{lognIpas}
                where user_idnt = #{userIdnt}
        </update>

    </mapper>
